{{- include "ffc-helm-library.container-config-map" (list . "rules-engine-poc-jbpm.config-map") -}}
{{- define "rules-engine-poc-jbpm.config-map" -}}
data:
  NODE_ENV: {{ quote .Values.environment }}
  PORT: {{ quote .Values.container.port }}
  APPINSIGHTS_CLOUDROLE: {{ quote .Values.name }}
{{- end -}}
